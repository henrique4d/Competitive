#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<pair<long long int, long long int>> v(n);
    for (int i=0; i<n; i++){
        cin >> v[i].first;
        v[i].first*=-1;
        
    }
  
}